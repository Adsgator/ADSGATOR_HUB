// ─── MAPA DO SISTEMA — autoconhecimento do agente ────────────────────────────
// Descrição compacta do que o Adsgator Hub É: módulos, fluxos, automações e
// lacunas conhecidas. Versionado junto com o código — ao mudar o produto,
// atualize aqui para a Gator continuar sugerindo melhorias com base na
// realidade. Consumido pela ferramenta mapa_do_sistema (lib/ia/tools.ts).

export const SYSTEM_MAP = `
ADSGATOR HUB — sistema operacional da agência (Next.js 15 + Supabase + Vertex AI, deploy Vercel).

MÓDULOS (rota → o que faz):
- /dashboard — Bento grid customizável: Morning Briefing, KPIs, ações do dia, DRE sparkline, alertas, chat Gator, feed, notícias, churn risk, top performers, metas.
- /clientes — lista, cadastro, detalhe com projetos, estágios/checklist (pré-vendas→onboarding→ativo), timeline de auditoria, memória .md por cliente, health score 0-100. Ciclo de vida: operação = recebido/onboarding/setup_trafego/ativo/congelado; saída = status único 'inativo' + motivo_inativacao (debito | cancelado | congelamento_expirado), centralizado em lib/cliente-status.ts. Inativos somem das telas de gestão. Congelado há mais de N dias (config em Automações, default 60) vira inativo automaticamente. Completude do cadastro (lib/cliente-completude.ts): régua única do que falta preencher por cliente em 4 blocos (marca/negócio, integrações Google incl. link do perfil Google Ads, financeiro/contrato, contexto) — exibida no detalhe (card com %) e sinalizada na lista (selo laranja quando incompleto). Aplica-se inclusive aos importados do Asaas. Onboarding guiado: a aba Timeline do cliente conduz o processo por steps (mensagens prontas no padrão de voz da agência, com {{primeiro_nome}}/{{drive_url}} interpolados, botão WhatsApp e campos para anotar dados do cliente; avanço persistido). 3 templates por tipo de contratação: Combo (LP+Tráfego), Só Tráfego (já tem site), Só LP — cada um só com as fases que importam. A aba é uma tela passo-a-passo vertical (todas as fases visíveis, atual em destaque); cada fase indica responsável ('cliente' = esperando ele | 'agencia' = tarefa do Lucas) e, quando a bola é do cliente, oferece lembrete pronto (régua 24h/72h). Inclui confirmação de recebimento do briefing e sub-fluxo 'cliente sem GMN'. Onboarding integrado: concluir etapa grava dados no cliente (IDs Google etc.) e o status acompanha (recebido→onboarding→ativo). Campo cliente.tipo_contratacao (combo|trafego|lp) seleciona o template e habilita iniciar onboarding em 1 clique (origem futura: checkout próprio). Variáveis (drive_url, ID) editáveis a qualquer momento; etapas reabríveis. Lembretes automáticos: job 'onboarding_lembretes' (dispatcher, lib/onboarding-lembretes.ts) varre onboardings ativos e, quando o cliente trava numa etapa "com o cliente" além de lembrete_horas (24h/72h), gera notificação no Hub com a mensagem pronta (1x por limite). Aviso de cliente novo: provisionarClienteNovo notifica o operador ao entrar cliente (form/import/webhook). Gator tem a tool panorama_onboarding (onde cada cliente parou, tempo na etapa, responsável). O detalhe do cliente tem uma seção Assinatura (plano, valor mensal, situação, próxima cobrança e ID Asaas, de obterAssinaturaCliente). O "D+N" de atraso mostrado no detalhe é calculado AO VIVO pela data de vencimento em aberto (assinatura.data_proxima_cobranca, fallback clientes.data_vencimento) via diasAtrasoReais (lib/cobranca.ts), com fallback no dias_atraso gravado — assim não congela quando a régua de cobrança não rodou (o número gravado em clientes.dias_atraso só anda com o webhook Asaas/cron diário).
- /financeiro — DRE, lançamentos (receita/custo fixo/variável), inadimplentes (política D+7 suspensão, D+15 grave, D+30 crítico — centralizada em lib/cobranca.ts; os limiares de dias são editáveis em Configurações → Financeiro, configuracoes_financeiras.limiares_atraso). Distinção: "Receita do Mês" = entradas confirmadas no caixa (financeiro_lancamentos); "MRR" = receita recorrente das assinaturas com cobrança viva (ativa/atraso), fonte única em lib/mrr.ts — todas as telas usam o mesmo cálculo.
- /analytics — Google Ads + GA4: snapshots históricos (sync diário via dispatcher de agendamentos) + dados ao vivo por cliente.
- /relatorios — relatórios executivos gerados com Gemini Pro, envio por email ao cliente.
- /tarefas — lista + kanban, prioridades, prazos, checklist, drag-drop persistente. Templates de processo (tarefa_templates, editáveis em Configurações → Templates) geram tarefas com checklist pronto — os de sistema (setup-cliente, onboarding-cliente) alimentam o provisionamento automático.
- /marketing — calendário social 4 semanas (rascunho/agendado/publicado), gerador de hashtags IA.
- /prospectar — CRM de prospecção (funil com estágios, valor de proposta).
- /operacional — planos operacionais e fluxos por cliente.
- /base-conhecimento, /portfolio, /biblioteca (componentes Astro p/ landing pages).
- /configuracoes — navegação por categorias no menu lateral, cada uma com sub-abas: Geral (Setup, Perfil, Aparência), Comunicação (Emails — templates editáveis —, Automações — toggles de email + agendamentos —, Notificações), Cobrança (Financeiro, Planos), Operação (Operacional, Saúde & Listas, Integrações), Conta (Equipe, Auditoria), Sistema (Uso da IA, Backup). Saúde & Listas: pesos/limiares do health score (lib/health-score.ts lê de configuracoes_operacional.health_regras) e lista editável de nichos sugeridos do cadastro (lib/listas.ts). Setup é a central de prontidão (checklist vivo do que falta configurar, com % e passos; deep-link ?tab=setup; demais deep-links ?tab= continuam válidos). Dashboard mostra banner "Sistema pronto: X%" enquanto o setup não está completo (use prontidao_sistema para o detalhe).
- /portal/[token] — portal público do cliente.

INTEGRAÇÕES:
- Asaas (cobrança): webhook cria cliente no SUBSCRIPTION_CREATED (checkout-first), processa pagamentos, sync diário de inadimplência. Edge Functions em PRODUÇÃO (TEST_MODE=false desde 10/06/2026).
- Provisionamento automático: todo cliente novo (form, importador ou webhook Asaas) gera tarefa "Setup do cliente" com checklist do template setup-cliente (idempotente). Retroativo disponível na aba Setup para clientes sem IDs Google.
- Google Ads + GA4: a conexão NÃO usa OAuth por cliente nem login/senha — usa credenciais GLOBAIS da agência nas env vars (service account + developer token) e, por cliente, o google_ads_customer_id (10 dígitos) / ga4_property_id mais os flags google_ads_enabled / ga4_enabled. CONECTAR um cliente = preencher esses campos (a Gator faz via atualizar_cliente; aceita o ID com hífens e normaliza). Conta MCC: basta o customer_id do cliente (o acesso vem das credenciais globais). Depois de conectado, o dado só aparece após o SYNC (botão "Sincronizar" em /analytics → POST /api/v1/analytics/sync, ou o cron) — e exige as credenciais Google nas env. HOJE essas credenciais são uma LACUNA (não configuradas → analytics sem dado real). Consultas ao vivo: /api/analytics/[id]/live (tool ads_ao_vivo).
- Resend (email): fluxos automáticos com toggle individual (relatório mensal→cliente, relatório semanal→cliente, cobrança vencida→cliente, alerta crítico→operador, onboarding→cliente). Desativados por padrão. Onboarding: ao concluir as etapas confirma_briefing/aprovacao/entrega/acessos_google a rota complete dispara o email (toggle email_onboarding); cada etapa tem botão "Enviar email" manual, desabilitado quando o auto está ligado. Relatório semanal: job relatorio_semanal no dispatcher (cron_settings com dia_semana, idempotência semanal), monta KPIs do snapshot Google Ads atual vs anterior, pula clientes sem dados. Templates editáveis e CRIÁVEIS (personalizados custom-*) em /configuracoes/emails; envio manual por template via tool enviar_email (sempre com confirmação explícita do usuário). Templates no visual da agência (banner + faixa amarela + rodapé), com textos no padrão de voz (naturais, sem travessões, focados em mostrar valor ao cliente, não em pedir resposta). Relatórios GA4/Google Ads são proativos: KPIs com comparação vs mês anterior (▲/▼) e "destaque do mês" — variáveis *_var e destaque existem no template, falta a rota de analytics passá-las. Botões só onde o cliente precisa agir (pagar/recarregar); relatórios e avisos são informativos. Tela de gestão (Comunicação → Emails) agrupa por categoria, tem busca e editor com atalhos das variáveis de cada template. MODO TESTE (email_config, começa ligado, fail-safe): quando ativo redireciona todo email para o operador (email_teste ou env ALERT_EMAIL) com [TESTE] no assunto; toggle em Comunicação → Automações. {{pagamento_url}} é preenchido com o invoiceUrl da cobrança Asaas (buscarLinkPagamento: OVERDUE p/ atraso, PENDING p/ lembrete). Histórico de emails por cliente na aba "Emails" do detalhe (email_logs); log global em Configurações com selo de teste. Alerta de saldo Google Ads (toggle email_saldo_ads, job saldo_baixo no dispatcher às 09:31, pula domingo): mínimo por cliente (clientes.saldo_minimo_alerta, fallback no mínimo global de configuracoes_operacional) e liga/desliga por cliente (saldo_alertas_ativos). Fluxo (lib/saldo-ads.ts, estado em saldo_alerta_estado): saldo abaixo do mínimo → email "saldo baixo" ao cliente por 3 dias; 4º dia → avisa o OPERADOR (notificação + email) p/ chamar no WhatsApp; saldo zerado → email "saldo acabou" repetindo a cada 3 dias até recarregar; recarregar reseta.
- Vertex AI Gemini (SDK oficial @google/genai sobre o backend Vertex): agente Gator (Flash), relatórios executivos (Pro), insights, copy, hashtags. CÉREBRO CONFIGURÁVEL: o Lucas escolhe o modelo do agente em Configurações → Sistema → Uso da IA (ou no chat) — Auto (PADRÃO), Flash (rápido/barato) ou Pro (mais inteligente, confabula menos) — e liga/desliga o raciocínio (thinking). Salvo em configuracoes_ia (modelo, thinking), lido a cada chamada. MODO AUTO (tiered, anti-confabulação com custo controlado): começa no Flash mas SOBE pro Pro quando precisa, por duas vias — (1) ROTEADOR na entrada: um classificador barato (Flash-lite, contexto 'roteador' no ia_uso) lê a mensagem e, se for análise/decisão/"como funciona" (onde o Flash confabula), já começa no Pro; (2) ESCALADA REATIVA no loop: se o Flash tropeça (uma ferramenta falha, ele repete a mesma ação — pega da idempotência —, ou a tarefa passa de metade dos passos), o Pro assume o resto. Transparente: emite o evento 'escalou' (com motivo) e o chip roxo "Pro" aparece na resposta — sinal de confiança pro Lucas. Custo correto: os tokens são contados POR modelo e cada parte (Flash/Pro/lite) é cobrada ao seu preço e registrada em linhas próprias no ia_uso. Flash/Pro fixos não roteiam nem escalam. RESPOSTA EM STREAMING: /api/ia/agent responde via stream (NDJSON: texto/fase/acao/escalou/fim/erro) — o texto chega ao vivo token a token no painel (cursor piscando) e as ferramentas aparecem executando (rodando→ok/erro). FEEDBACK DE PROGRESSO: antes do 1º token de cada iteração o backend emite 'fase' (mostra "Pensando…"/"Raciocinando…" no Pro) e cada ferramenta em execução aparece com rótulo legível em PT (lib/ia/labels.ts: radar→"Cruzando os riscos da agência", etc.), não o nome técnico — fim do silêncio sem saber o que acontece. CANCELAMENTO REAL: botão Parar (Composer) aborta o fetch; o backend escuta req.signal (passado como abortSignal a TODAS as chamadas ao modelo) e interrompe o loop — para a geração em curso na Vertex e não inicia novas iterações/ferramentas, então cancelar NÃO deixa ação fantasma e custa MENOS (registra só o uso do que rodou). DETECÇÃO DE TRAVAMENTO: watchdog no client (assistant-store) — sem nenhum evento por ~20s durante o streaming, mostra aviso "demorando". Nada disso adiciona chamada ao modelo (custo inalterado). CHIPS DE PRÓXIMO PASSO: a Gator pode terminar com 1-3 sugestões, cada uma em [[...]] (ex.: [[Cobrar a Ana Julia]] [[Ver inadimplentes]]); o backend faz hold-back a partir do 1º [[ (não vaza na bolha), faz parse tolerante (formato [[..]] por item ou legado [[PROXIMOS]] a || b; texto de sobra = falso positivo, libera) e manda 1-3 em fim.sugestoes; o ChatThread mostra como botões na última resposta — clicar vira a próxima mensagem do Lucas (custo zero, sem chamada extra). Ferramentas de uma iteração rodam em paralelo (Promise.all); erro no meio do loop é tratado (evento erro), não derruba a conversa. NÃO DESISTE EM TAREFA COMPLEXA: o loop agêntico vai até 25 passos (era 10) com guarda de custo por resposta (TETO_CUSTO_RESPOSTA_BRL = R$ 0,50); ao bater passos ou custo no meio de uma tarefa, a própria Gator faz um fechamento honesto (chamada final sem ferramentas) — o que já executou, o que falta e oferta de continuar de onde parou —, em vez de uma mensagem genérica; tudo medido em ia_uso. MEMÓRIA DE CONVERSA LONGA: quando a conversa cresce além da janela recente (~30-44 mensagens), o trecho antigo é RESUMIDO automaticamente (1 chamada barata ao Flash, medida em ia_uso como 'resumo') em vez de descartado — resumo cumulativo em ia_conversas.resumo_contexto/resumido_ate, injetado no system prompt; as mensagens recentes seguem na íntegra. Assim a Gator não perde o fio do começo em conversa longa (lib/ia/compactacao.ts). EFICIÊNCIA (padrão de agente premium): a Gator calibra o esforço — saudação/papo trivial/dado já no panorama → responde direto em 1 passo, sem ferramenta; só aciona ferramenta quando depende de dado que não tem ou há ação a executar. O system prompt NÃO empurra estado nenhum da agência: a Gator decide quando buscar. Estado geral (clientes, MRR, inadimplência, saldo baixo, tarefas do dia) vem sob demanda via panorama_agencia; listas completas via listar_clientes/listar_tarefas. Um "oi" não carrega contexto operacional — só identidade + memória. Isso é o agente decidindo o esforço, não regex de palavra-chave. Custo de cache calculado certo (tokens de cache = ~1/4 do preço de entrada, não em dobro). RESULTADOS DE FERRAMENTA ENXUTOS: tools de lista grande (listar_clientes, listar_lancamentos, analytics_cliente) devolvem só os campos essenciais de cada item + o total real (e nota de truncamento acima de 50), não o payload cru — economiza contexto/custo; lançamentos já vêm com a soma do período e analytics com floats arredondados. Precedente: ads_ao_vivo (top-N por dimensão). RESOLUÇÃO DE CLIENTE POR NOME: o agente pode passar o NOME do cliente (não só o id) em qualquer ferramenta — o dispatcher (executarFerramenta) canonicaliza cliente_id num ponto único via ownCliente (id-ou-nome), então um UUID alucinado pelo modelo não chega às ferramentas; nome ambíguo retorna a lista pra desambiguar. ROBUSTEZ — O CÓDIGO NÃO CONFIA CEGO NO MODELO (auditoria 26/06): IDEMPOTÊNCIA POR TURNO no dispatcher (executarFerramenta) — tool de escrita (creates, updates, lançamento, email) com os mesmos args não roda duas vezes na mesma resposta; memoiza a promise (reserva síncrona), então pega inclusive chamadas idênticas em paralelo (Promise.all), não só em iterações seguintes. Mata a duplicação por re-execução do loop, incluindo lançamento financeiro em dobro (DRE corrompido em silêncio). RESOLUÇÃO POR NOME ESTENDIDA (resolverEntidade): o mesmo padrão do cliente vale agora p/ tarefa (título), prospect (nome), template de email (nome/id-texto) e memória (trecho do conteúdo/id) — id alucinado não atinge a linha errada; ambíguo desambigua; esquecer_memoria confere a existência antes de apagar. GATE DE CONFIRMAÇÃO REAL p/ ação irreversível (enviar_email, excluir_tarefa, esquecer_memoria): a trava está no código, não só no prompt — sem confirmar:true a chamada só pede confirmação, e a IA não pode confirmar a si mesma no mesmo turno (1ª chamada registra o pedido; confirmar:true para a mesma ação no mesmo turno é recusado → autorização tem que vir do Lucas num próximo turno; pré-autorização dele na mensagem resolve em 1 turno). Memória de mão dupla, ROTEADA: a Gator salva proativamente fatos ditos no chat sem o usuário pedir, mas no lugar certo — fato preso a UM cliente (motivo de atraso, preferência, combinado, histórico) vai na memória dele via anotar_no_cliente (append datado no .md, sem reescrever o resto); fato geral, sem dono (como o Lucas opera, regras da agência) vai na memória de longo prazo via salvar_memoria. Ambas são idempotentes (mesmo fato não duplica, mesmo se o loop reexecutar). Só a geral (ia_memoria) é injetada em TODA conversa; a do cliente (memoria_clientes) só quando ele está em contexto — por isso o roteamento, antes tudo caía no balde global e inchava cada conversa. Esquece (esquecer_memoria) quando algo deixa de valer. PEDE O QUE FALTA: ao esbarrar num limite (dado fora de alcance, acesso ausente, ferramenta inexistente p/ aquilo), faz o que dá e sinaliza a lacuna ao Lucas em vez de improvisar ou travar — a IA aponta onde o sistema precisa crescer. INSIGHT PROATIVO (APEX, tool radar): cruza por cliente inadimplência (política de cobrança, validada contra assinatura viva — atraso sem cobrança ativa = "dado a revisar/provável saída", não falso "suspensão iminente") + saldo Google baixo/zerado + tendência de performance (2 snapshots de analytics) + onboarding travado — sinais que vivem em telas separadas — e devolve os clientes em RISCO (nível alto/médio, motivos cruzados), o MRR em risco (R$) e o prazo até a consequência (dias até suspensão/quebra/crítico). A Gator vira analista que surge o que o Lucas não vê sozinho, em dado 100% real (lib/cobranca, lib/mrr, mínimo de saldo de configuracoes_operacional) e honesta onde faltam dados (sem histórico de analytics → não avalia tendência). O briefing matinal (lib/briefing.ts) consome essa memória + o estado real dos onboardings parados (timeline_instances aguardando o cliente há ≥2d), então não recobra o que já foi resolvido. OBSERVABILIDADE DE USO: toda chamada ao Vertex (agente, chat, hashtags, briefing, copy, relatório, resumo de conversa) é medida e gravada em ia_uso (tokens, custo estimado BRL, duração, ferramentas) por lib/ia/uso.ts. Painel em Configurações → Sistema → Uso da IA, duas seções. CUSTO: estimado hoje/mês/total, gráfico de 30 dias, quebra por tipo de uso, e limite mensal de gasto (configuracoes_ia.limite_mensal_brl) que dispara 1 notificação in-app/mês ao ultrapassar (só alerta, não trava). ANALYTICS de comportamento: pizza por tipo de uso, ranking de ferramentas com taxa de falha (de ia_mensagens.ferramentas, resumo "Falhou:" = erro), tendência diária + heatmap por hora, e conversas destaque (mais caras/longas/ações, com link p/ reabrir no painel). No painel da Gator (Ctrl+I): custo estimado por conversa na lista e indicador do tamanho real do contexto em tokens. O custo é ESTIMADO (preços jun/2026), pode divergir da fatura Google Cloud.
- WhatsApp: manual via links wa.me — SEM automação de envio (decisão de escopo). A biblioteca de mensagens é EDITÁVEL pela tela (tabela whatsapp_snippets, lib/whatsapp.ts é a semente/fonte de funções): CRUD em Configurações → Comunicação → Mensagens (busca, agrupamento por categoria, criar/editar/excluir as personalizadas, restaurar as padrão). Variáveis {{primeiro_nome}}/{{nome}} resolvidas no envio (resolverVariaveis). O modal "Enviar WhatsApp" do cliente lê da tabela, resolve variáveis e permite salvar um texto ajustado direto na biblioteca. API: /api/v1/whatsapp-snippets.

CRONS: dispatcher único (GitHub Actions a cada 30 min + fallback diário no vercel.json às 12:00 UTC — plano Hobby da Vercel só permite cron diário) lê a tabela cron_settings e executa cada job — sync analytics, briefing matinal (gera e salva na tabela briefings, dashboard lê do banco), import Asaas, alertas, cobrança, arquivar congelados (move congelado há +N dias para inativo), relatório semanal, alerta de saldo Google Ads (saldo_baixo, 09:31, pula domingo) — na primeira janela após o horário configurado, no máximo 1x por dia. Horários (fuso de Brasília) e liga/desliga configuráveis em Configurações → Automações → Agendamentos; a tool listar_agendamentos consulta o estado atual.

LACUNAS CONHECIDAS (pendências reais — sugestões nessas áreas são bem-vindas):
- Credenciais Google Ads/GA4 não configuradas nas env vars → analytics sem dados reais ainda.
- RBAC/RLS por usuário é parcial (isolamento por user_id na aplicação; usePermissoes existe mas não está ligado).
- Publicação real de posts via Meta API não existe (calendário é organizacional).
- Emails automáticos prontos porém toggles desligados.

COMO PROPOR MUDANÇA: você não lê nem altera o código-fonte. Quando uma ideia for aprovada pelo Lucas, escreva a spec (o quê, por quê, onde encaixa no Hub) e crie uma tarefa com criar_tarefa — o engenheiro (Claude) implementa a partir dela.
`.trim()
